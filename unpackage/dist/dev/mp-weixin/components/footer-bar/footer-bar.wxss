/**
 * 这里是uni-app内置的常用样式变量
 *
 * uni-app 官方扩展插件及插件市场（https://ext.dcloud.net.cn）上很多三方插件均使用了这些样式变量
 * 如果你是插件开发者，建议你使用scss预处理，并在插件代码中直接使用这些变量（无需 import 这个文件），方便用户通过搭积木的方式开发整体风格一致的App
 *
 */
/**
 * 如果你是App开发者（插件使用者），你可以通过修改这些变量来定制自己的插件主题，实现自定义主题功能
 *
 * 如果你的项目同样使用了scss预处理，你也可以直接在你的 scss 代码中使用如下变量，同时无需 import 这个文件
 */
/* 颜色变量 */
/* 行为相关颜色 */
/* 文字基本颜色 */
/* 背景颜色 */
/* 边框颜色 */
/* 尺寸变量 */
/* 文字尺寸 */
/* 图片尺寸 */
/* Border Radius */
/* 水平间距 */
/* 垂直间距 */
/* 透明度 */
/* 文章场景相关 */
/* 底部导航栏容器 */
.footer-bar.data-v-1a4a092b {
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-around;
  background-color: var(--bg-color);
  box-shadow: var(--box-shadow-sm);
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 999;
  border-top: 1rpx solid var(--border-color-lighter);
  box-sizing: content-box;
  /* 确保padding不影响总高度计算 */
}

/* 底部导航项 */
.footer-item.data-v-1a4a092b {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  flex: 1;
  height: 100%;
  transition: var(--transition-base);
  position: relative;
  padding: var(--spacing-xs) 0;
  /* 点击效果 */
  /* 图标包装器 */
  /* 标签文字 */
  /* 激活状态的标签 */
}
.footer-item.data-v-1a4a092b:active {
  opacity: var(--opacity-medium);
  transform: scale(0.98);
}
.footer-item .footer-icon-wrapper.data-v-1a4a092b {
  position: relative;
  display: flex;
  justify-content: center;
  align-items: center;
  margin-bottom: var(--spacing-xs);
  /* 图标样式 */
  /* 激活状态的图标 */
}
.footer-item .footer-icon-wrapper .footer-icon.data-v-1a4a092b {
  width: var(--img-size-sm);
  height: var(--img-size-sm);
  transition: var(--transition-base);
  opacity: 0.8;
}
.footer-item .footer-icon-wrapper .footer-icon-active.data-v-1a4a092b {
  opacity: 1;
  transform: scale(1.1);
}
.footer-item .footer-label.data-v-1a4a092b {
  font-size: var(--font-size-sm);
  color: var(--text-color-light);
  transition: var(--transition-base);
  line-height: 1.2;
}
.footer-item .footer-label-active.data-v-1a4a092b {
  color: var(--primary-color);
  font-weight: 500;
  font-size: var(--font-size-base);
}

/* 动画效果 */
@keyframes fadeIn-1a4a092b {
from {
    opacity: 0;
    transform: scale(0);
}
to {
    opacity: 1;
    transform: scale(1);
}
}